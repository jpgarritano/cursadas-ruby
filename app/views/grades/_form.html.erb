<%= simple_form_for [@grade.test.course, @grade.test, @grade] do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :grade, label: 'Nota'  %>
    <%= f.association :student, collection: @students , label: 'Estudiante' %>
    <%= f.association :test, label: 'Evaluación' %>
  </div>

  <div class="form-actions">
    <%= f.button :submit ,"Aceptar", class: 'btn btn-success'%>
  </div>
<% end %>
