<div class="row justify-content-center ">

<div class="col-6 bg-light">
<h1>Cargar Nota para <%= @grade.test %></h1>

<%= render 'form', grade: @grade %>

<%= link_to 'Volver', course_test_grades_path %>
</div>
</div>