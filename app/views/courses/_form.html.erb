<%= simple_form_for(@course) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :year, label: "Año", collection: @years%>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Aceptar", class: 'btn btn-success' %>
  </div>
<% end %>
